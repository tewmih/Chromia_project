add,update{
    title,
    description,
    priority,
    duedate
}

connectWallet{

}

delete{

}

completeupdate{

}
complatefiler{

}

disconnectWallet{//i dont know

}
upcoming{
    filter by duedate
}
filters{
    
}

status: 0 | 1; 
in to do type
      await session?.call<any>("update_task_status", {
        task_id: task.id,
        status: status === 1 ? 0: 1,
      });

      console.log("Task status updated successfully");
      setStatus((prevStatus) =>
        prevStatus === 1 ? 0 : 1
        in taskBox